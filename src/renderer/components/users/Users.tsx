import * as React from 'react';
import { PageMain, PageTitle } from '../generic-styled-components/Page';

/**
 * @description this is the main component for the users screen
 */
export const Users = () => {
  return (
    <PageMain>
      <PageTitle>Users</PageTitle>
    </PageMain>
  );
};
